import Generations from "../Generations";
import Logo from "../Logo";

const Header = () => {
  return (
    <div>
      <h1>Component Header</h1>
      <Logo />
      <Generations />
    </div>
  );
};

export default Header;
